@* Edit Shareholder Modal *@
<div class="modal-overlay" id="editShareholderModal" style="display: none;">
    <div class="modal-dialog-custom">
        <div class="modal-content-custom">
            <div class="modal-header-custom">
                <span class="modal-title-custom">Edit Shareholder</span>
                <button type="button" class="btn-close-custom" onclick="closeEditModal()">?</button>
            </div>
            <div class="modal-body-custom">
                <form id="editShareholderForm">
                    <input type="hidden" id="edit_shareholderId" />
                    
                    @* Shareholder Information Section *@
                    <div class="form-section">
                        <div class="section-header">
                            <span class="section-title">Shareholder Information</span>
                            <span class="section-chevron">?</span>
                        </div>
                        <div class="section-content">
                            <div class="form-row">
                                <div class="form-group">
                                    <label>First Name</label>
                                    <input type="text" id="edit_firstName" placeholder="Primera">
                                </div>
                                <div class="form-group">
                                    <label>Last name</label>
                                    <input type="text" id="edit_lastName" placeholder="Weerakon">
                                </div>
                                <div class="form-group">
                                    <label>Country</label>
                                    <select id="edit_country">
                                        <option>SriLanka</option>
                                        <option>India</option>
                                        <option>USA</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label>Folio ID</label>
                                    <input type="text" id="edit_folionId" placeholder="FLN565" readonly>
                                </div>
                            </div>
                            <div class="form-row">
                                <div class="form-group full-width">
                                    <label>Full name(Passport Name)</label>
                                    <input type="text" id="edit_fullName" placeholder="Chamalie Primera  Weerakoon">
                                </div>
                            </div>
                            <div class="form-row">
                                <div class="form-group">
                                    <label>Address 1</label>
                                    <input type="text" id="edit_address1" placeholder="Malabe">
                                </div>
                                <div class="form-group">
                                    <label>Address 2</label>
                                    <input type="text" id="edit_address2" placeholder="SriLanka">
                                </div>
                                <div class="form-group">
                                    <label>City</label>
                                    <input type="text" id="edit_city" placeholder="Malabe">
                                </div>
                                <div class="form-group">
                                    <label>company/individual</label>
                                    <select id="edit_companyIndividual">
                                        <option>Srilankan Airlines</option>
                                        <option>Individual</option>
                                    </select>
                                </div>
                            </div>
                            <div class="form-row">
                                <div class="form-group">
                                    <label>No. of shares</label>
                                    <div class="number-input">
                                        <button type="button" onclick="decrementEditValue('edit_noOfShares')">-</button>
                                        <input type="text" id="edit_noOfShares" value="00" readonly>
                                        <button type="button" onclick="incrementEditValue('edit_noOfShares')">+</button>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label>No. of Tickets Issued</label>
                                    <div class="number-input">
                                        <button type="button" onclick="decrementEditValue('edit_noOfTickets')">-</button>
                                        <input type="text" id="edit_noOfTickets" value="00" readonly>
                                        <button type="button" onclick="incrementEditValue('edit_noOfTickets')">+</button>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label>Entitlement</label>
                                    <div class="entitlement-input">
                                        <select id="edit_entitlementLetter">
                                            <option value="">-</option>
                                            <option value="A">A</option>
                                            <option value="B">B</option>
                                            <option value="C">C</option>
                                            <option value="D">D</option>
                                            <option value="E">E</option>
                                            <option value="F">F</option>
                                            <option value="G">G</option>
                                            <option value="H">H</option>
                                            <option value="I">I</option>
                                            <option value="J">J</option>
                                            <option value="K">K</option>
                                            <option value="L">L</option>
                                            <option value="M">M</option>
                                            <option value="N">N</option>
                                            <option value="O">O</option>
                                            <option value="P">P</option>
                                            <option value="Q">Q</option>
                                            <option value="R">R</option>
                                            <option value="S">S</option>
                                            <option value="T">T</option>
                                            <option value="U">U</option>
                                            <option value="V">V</option>
                                            <option value="W">W</option>
                                            <option value="X">X</option>
                                            <option value="Y">Y</option>
                                            <option value="Z">Z</option>
                                        </select>
                                        <input type="number" id="edit_entitlementNumber" min="0" max="99" placeholder="00">
                                    </div>
                                </div>
                            </div>
                            <div class="add-btn-row">
                                <button type="button" class="btn-add" onclick="addEditPassportItem()">
                                    <span>+</span> Add Passport
                                </button>
                            </div>
                        </div>
                    </div>

                    @* Add Passport Information Section *@
                    <div class="form-section">
                        <div class="section-header">
                            <span class="section-title">Add Passport Information</span>
                            <span class="section-chevron">?</span>
                        </div>
                        <div class="section-content">
                            <div class="items-list" id="edit_passportList">
                                @* Passport items will be added here dynamically *@
                            </div>
                            <div class="counter-row">
                                <span class="counter-text">No. of Passports : <span id="edit_passportCount">00</span></span>
                            </div>
                            <div class="add-btn-row">
                                <button type="button" class="btn-add" onclick="addEditDependentItem()">
                                    <span>+</span> Add Dependant
                                </button>
                            </div>
                        </div>
                    </div>

                    @* Dependent Passport Information Section *@
                    <div class="form-section">
                        <div class="section-header">
                            <span class="section-title">Dependent Passport Information</span>
                            <span class="section-chevron">?</span>
                        </div>
                        <div class="section-content">
                            <div class="items-list" id="edit_dependentList">
                                @* Dependent items will be added here dynamically *@
                            </div>
                            <div class="counter-row">
                                <span class="counter-text">No. of Dependants : <span id="edit_dependentCount">00</span></span>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer-custom">
                <button type="button" class="btn-save" onclick="updateShareholder()">Update</button>
            </div>
        </div>
    </div>
</div>

<style>
    .modal-overlay {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.4);
        z-index: 9999;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
    }

    .modal-dialog-custom {
        max-width: 900px;
        width: 100%;
        max-height: 92vh;
        overflow-y: auto;
        scrollbar-width: none;
        -ms-overflow-style: none;
    }

    .modal-dialog-custom::-webkit-scrollbar {
        display: none;
    }

    .modal-content-custom {
        background: white;
        border-radius: 12px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
        overflow: hidden;
    }

    .modal-header-custom {
        background: linear-gradient(135deg, #1e3a8a 0%, #2d4a9a 100%);
        color: white;
        padding: 14px 20px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .modal-title-custom {
        font-size: 15px;
        font-weight: 500;
    }

    .btn-close-custom {
        background: none;
        border: none;
        color: white;
        font-size: 20px;
        cursor: pointer;
        line-height: 1;
        padding: 0;
    }

    .btn-close-custom:hover {
        opacity: 0.8;
    }

    .modal-body-custom {
        padding: 28px;
        background: white;
        max-height: calc(92vh - 120px);
        overflow-y: auto;
        scrollbar-width: none;
        -ms-overflow-style: none;
    }

    .modal-body-custom::-webkit-scrollbar {
        display: none;
    }

    .form-section {
        margin-bottom: 20px;
    }

    .section-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-bottom: 8px;
        border-bottom: 1px solid #e5e7eb;
        margin-bottom: 16px;
        cursor: pointer;
    }

    .section-title {
        font-size: 13px;
        font-weight: 600;
        color: #1f2937;
    }

    .section-chevron {
        font-size: 10px;
        color: #9ca3af;
    }

    .section-content {
        padding: 0;
    }

    .form-row {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 16px;
        margin-bottom: 14px;
    }

    .form-group {
        display: flex;
        flex-direction: column;
    }

    .form-group.full-width {
        grid-column: span 4;
    }

    .form-group label {
        font-size: 11px;
        font-weight: 500;
        color: #374151;
        margin-bottom: 4px;
    }

    .form-group input,
    .form-group select {
        padding: 8px 10px;
        border: 1px solid #e5e7eb;
        border-radius: 4px;
        font-size: 12px;
        color: #6b7280;
        background: white;
    }

    .form-group input:focus,
    .form-group select:focus {
        outline: none;
        border-color: #1e3a8a;
    }

    .form-group input::placeholder {
        color: #9ca3af;
    }

    .form-group input[readonly] {
        background: #f9fafb;
        color: #6b7280;
    }

    .number-input {
        display: flex;
        border: 1px solid #e5e7eb;
        border-radius: 4px;
        overflow: hidden;
    }

    .number-input button {
        width: 32px;
        border: none;
        background: white;
        color: #6b7280;
        cursor: pointer;
        font-size: 14px;
    }

    .number-input button:hover {
        background: #f3f4f6;
    }

    .number-input input {
        flex: 1;
        border: none;
        border-left: 1px solid #e5e7eb;
        border-right: 1px solid #e5e7eb;
        text-align: center;
        padding: 8px;
        font-size: 12px;
        color: #6b7280;
    }

    .entitlement-input {
        display: flex;
        gap: 8px;
    }

    .entitlement-input select {
        width: 60px;
        padding: 8px;
        border: 1px solid #e5e7eb;
        border-radius: 4px;
        font-size: 12px;
        text-align: center;
        font-weight: 600;
    }

    .entitlement-input input {
        flex: 1;
        padding: 8px;
        border: 1px solid #e5e7eb;
        border-radius: 4px;
        text-align: center;
        font-size: 12px;
        color: #6b7280;
    }

    .entitlement-input input:focus {
        outline: none;
        border-color: #1e3a8a;
    }

    .item-box {
        border: 1px solid #1e3a8a;
        border-radius: 6px;
        padding: 14px;
        margin-bottom: 10px;
        position: relative;
    }

    .item-row {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 14px;
        padding-right: 24px;
    }

    .item-row-3 {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 14px;
        padding-right: 24px;
    }

    .item-group {
        display: flex;
        flex-direction: column;
    }

    .item-group label {
        font-size: 11px;
        font-weight: 500;
        color: #374151;
        margin-bottom: 4px;
    }

    .items-list {
        margin-bottom: 8px;
    }

    .add-btn-row {
        display: flex;
        justify-content: flex-end;
        margin-top: 12px;
        margin-bottom: 8px;
    }

    .btn-add {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 8px 16px;
        border: 1px solid #1e3a8a;
        background: white;
        color: #1e3a8a;
        border-radius: 4px;
        font-size: 12px;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.2s;
    }

    .btn-add:hover {
        background: #1e3a8a;
        color: white;
    }

    .btn-add span {
        font-size: 14px;
        font-weight: 600;
    }

    .modal-footer-custom {
        padding: 16px 24px;
        display: flex;
        justify-content: flex-end;
        background: white;
    }

    .btn-save {
        padding: 10px 32px;
        background: #1e3a8a;
        color: white;
        border: none;
        border-radius: 4px;
        font-size: 13px;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.2s;
    }

    .btn-save:hover {
        background: #1e40af;
    }
</style>

<script>
    let editPassportCounter = 0;
    let editDependentCounter = 0;

    function incrementEditValue(id) {
        const input = document.getElementById(id);
        let value = parseInt(input.value) || 0;
        value++;
        input.value = value.toString().padStart(2, '0');
    }

    function decrementEditValue(id) {
        const input = document.getElementById(id);
        let value = parseInt(input.value) || 0;
        if (value > 0) {
            value--;
            input.value = value.toString().padStart(2, '0');
        }
    }

    function addEditPassportItem() {
        editPassportCounter++;
        const passportHtml = `
            <div class="item-box passport-item">
                <button type="button" class="btn-remove" onclick="removeEditPassport(this)">?</button>
                <div class="item-row">
                    <div class="item-group">
                        <label>Passport number</label>
                        <input type="text" placeholder="600 5465758">
                    </div>
                    <div class="item-group">
                        <label>Issued country</label>
                        <input type="text" placeholder="Sri lanka">
                    </div>
                    <div class="item-group">
                        <label>Expire Date</label>
                        <input type="date" class="date-picker">
                    </div>
                    <div class="item-group">
                        <label>Issued date</label>
                        <input type="date" class="date-picker">
                    </div>
                </div>
            </div>
        `;
        document.getElementById('edit_passportList').insertAdjacentHTML('beforeend', passportHtml);
        updateEditPassportCount();
    }

    function addEditDependentItem() {
        editDependentCounter++;
        const dependentHtml = `
            <div class="item-box dependent-item">
                <button type="button" class="btn-remove" onclick="removeEditDependent(this)">?</button>
                <div class="item-row-3">
                    <div class="item-group">
                        <label>Full Name</label>
                        <input type="text" placeholder="Buddhini Nanayakkara">
                    </div>
                    <div class="item-group">
                        <label>Relationship</label>
                        <input type="text" placeholder="Daughter">
                    </div>
                    <div class="item-group">
                        <label>Passport number</label>
                        <input type="text" placeholder="600 5465758">
                    </div>
                </div>
            </div>
        `;
        document.getElementById('edit_dependentList').insertAdjacentHTML('beforeend', dependentHtml);
        updateEditDependentCount();
    }

    function updateEditPassportCount() {
        const count = document.querySelectorAll('#edit_passportList .passport-item').length;
        document.getElementById('edit_passportCount').textContent = count.toString().padStart(2, '0');
    }

    function updateEditDependentCount() {
        const count = document.querySelectorAll('#edit_dependentList .dependent-item').length;
        document.getElementById('edit_dependentCount').textContent = count.toString().padStart(2, '0');
    }

    function removeEditPassport(element) {
        element.closest('.passport-item').remove();
        updateEditPassportCount();
    }

    function removeEditDependent(element) {
        element.closest('.dependent-item').remove();
        updateEditDependentCount();
    }

    function closeEditModal() {
        document.getElementById('editShareholderModal').style.display = 'none';
        document.body.style.overflow = 'auto';
    }

    function loadShareholderData(folioId) {
        // Show loading state
        const modal = document.getElementById('editShareholderModal');
        modal.style.display = 'flex';
        document.body.style.overflow = 'hidden';

        // Fetch shareholder data with passports and dependents from server
        fetch('@Url.Action("GetShareholderWithDetails", "ShareholderListPage")?folioId=' + encodeURIComponent(folioId))
            .then(response => response.json())
            .then(result => {
                if (result.success && result.data) {
                    const shareholderData = result.data.shareholder;
                    const passports = result.data.passports || [];
                    const dependents = result.data.dependents || [];
                    
                    console.log('Loaded passports:', passports); // Debug log
                    
                    // Populate shareholder form fields
                    document.getElementById('edit_shareholderId').value = shareholderData.ShareholderID || '';
                    document.getElementById('edit_firstName').value = shareholderData.FirstName || '';
                    document.getElementById('edit_lastName').value = shareholderData.LastName || '';
                    document.getElementById('edit_fullName').value = shareholderData.FullName || '';
                    document.getElementById('edit_folionId').value = shareholderData.FolioID || '';
                    document.getElementById('edit_country').value = shareholderData.Country || 'SriLanka';
                    document.getElementById('edit_address1').value = shareholderData.Address1 || '';
                    document.getElementById('edit_address2').value = shareholderData.Address2 || '';
                    document.getElementById('edit_city').value = shareholderData.City || '';
                    document.getElementById('edit_companyIndividual').value = shareholderData.CompanyIndividual || 'Individual';
                    document.getElementById('edit_noOfShares').value = (shareholderData.NoOfShares || 0).toString().padStart(2, '0');
                    document.getElementById('edit_noOfTickets').value = (shareholderData.NoOfTicketsIssued || 0).toString().padStart(2, '0');
                    
                    // Parse entitlement value (e.g., "A05" -> letter="A", number="05")
                    const entitlement = shareholderData.Entitlement || '';
                    if (entitlement.length > 0) {
                        const letter = entitlement.charAt(0); // First character
                        const number = entitlement.substring(1); // Rest of the string
                        document.getElementById('edit_entitlementLetter').value = letter;
                        document.getElementById('edit_entitlementNumber').value = number;
                    } else {
                        document.getElementById('edit_entitlementLetter').value = '';
                        document.getElementById('edit_entitlementNumber').value = '';
                    }
                    
                    // Clear and load passports
                    document.getElementById('edit_passportList').innerHTML = '';
                    passports.forEach(passport => {
                        editPassportCounter++;
                        
                        // Handle both IssueDate and IssuedDate property names
                        const issueDate = passport.IssuedDate || passport.IssueDate;
                        const expiryDate = passport.ExpiryDate;
                        
                        console.log('Passport dates - Issue:', issueDate, 'Expiry:', expiryDate); // Debug log
                        
                        const passportHtml = `
                            <div class="item-box passport-item" data-passport-id="${passport.PassportID || ''}">
                                <button type="button" class="btn-remove" onclick="removeEditPassport(this)">?</button>
                                <div class="item-row">
                                    <div class="item-group">
                                        <label>Passport number</label>
                                        <input type="text" value="${escapeHtml(passport.PassportNumber || '')}" placeholder="600 5465758">
                                    </div>
                                    <div class="item-group">
                                        <label>Issued country</label>
                                        <input type="text" value="${escapeHtml(passport.IssuedCountry || '')}" placeholder="Sri lanka">
                                    </div>
                                    <div class="item-group">
                                        <label>Expire Date</label>
                                        <input type="date" class="date-picker" value="${formatDateForInput(expiryDate)}">
                                    </div>
                                    <div class="item-group">
                                        <label>Issued date</label>
                                        <input type="date" class="date-picker" value="${formatDateForInput(issueDate)}">
                                    </div>
                                </div>
                            </div>
                        `;
                        document.getElementById('edit_passportList').insertAdjacentHTML('beforeend', passportHtml);
                    });
                    updateEditPassportCount();
                    
                    // Clear and load dependents
                    document.getElementById('edit_dependentList').innerHTML = '';
                    dependents.forEach(dependent => {
                        editDependentCounter++;
                        const dependentHtml = `
                            <div class="item-box dependent-item" data-dependent-id="${dependent.DependentID || ''}">
                                <button type="button" class="btn-remove" onclick="removeEditDependent(this)">?</button>
                                <div class="item-row-3">
                                    <div class="item-group">
                                        <label>Full Name</label>
                                        <input type="text" value="${escapeHtml(dependent.FullName || '')}" placeholder="Buddhini Nanayakkara">
                                    </div>
                                    <div class="item-group">
                                        <label>Relationship</label>
                                        <input type="text" value="${escapeHtml(dependent.Relationship || '')}" placeholder="Daughter">
                                    </div>
                                    <div class="item-group">
                                        <label>Passport number</label>
                                        <input type="text" value="${escapeHtml(dependent.PassportNumber || '')}" placeholder="600 5465758">
                                    </div>
                                </div>
                            </div>
                        `;
                        document.getElementById('edit_dependentList').insertAdjacentHTML('beforeend', dependentHtml);
                    });
                    updateEditDependentCount();
                } else {
                    // Error loading data - close modal and reload to show notification
                    closeEditModal();
                    window.location.reload();
                }
            })
            .catch(error => {
                console.error('Error loading shareholder data:', error);
                // Network error - close modal and reload to show notification
                closeEditModal();
                window.location.reload();
            });
    }

    // Helper function to escape HTML to prevent XSS
    function escapeHtml(text) {
        if (!text) return '';
        const div = document.createElement('div');
        div.textContent = text;
        return div.innerHTML;
    }

    // Helper function to format date for input field (YYYY-MM-DD)
    function formatDateForInput(dateString) {
        if (!dateString) {
            console.log('Date is empty or null'); // Debug
            return '';
        }
        
        console.log('Formatting date:', dateString); // Debug
        
        try {
            // Handle various date formats
            let date;
            
            // If it's already in YYYY-MM-DD format
            if (typeof dateString === 'string' && /^\d{4}-\d{2}-\d{2}$/.test(dateString)) {
                console.log('Date is already in correct format');
                return dateString;
            }
            
            // Parse the date
            date = new Date(dateString);
            
            // Check if date is valid
            if (isNaN(date.getTime())) {
                console.log('Invalid date:', dateString);
                return '';
            }
            
            // Format to YYYY-MM-DD
            const year = date.getFullYear();
            const month = String(date.getMonth() + 1).padStart(2, '0');
            const day = String(date.getDate()).padStart(2, '0');
            const formatted = `${year}-${month}-${day}`;
            
            console.log('Formatted date:', formatted); // Debug
            return formatted;
        } catch (error) {
            console.error('Error formatting date:', error, dateString);
            return '';
        }
    }

    function updateShareholder() {
        // Collect form data
        const entitlementLetter = document.getElementById('edit_entitlementLetter')?.value || '';
        const entitlementNumber = document.getElementById('edit_entitlementNumber')?.value || '0';
        const formattedNumber = parseInt(entitlementNumber).toString().padStart(2, '0');
        const entitlementValue = entitlementLetter + formattedNumber; // e.g., "A05" or "B12"

        // Collect passports
        const passports = [];
        document.querySelectorAll('#edit_passportList .passport-item').forEach(item => {
            const inputs = item.querySelectorAll('input');
            const passportId = item.getAttribute('data-passport-id');
            passports.push({
                PassportID: passportId ? parseInt(passportId) : 0,
                PassportNumber: inputs[0]?.value || '',
                IssuedCountry: inputs[1]?.value || '',
                ExpiryDate: inputs[2]?.value || null,
                IssuedDate: inputs[3]?.value || null
            });
        });

        // Collect dependents
        const dependents = [];
        document.querySelectorAll('#edit_dependentList .dependent-item').forEach(item => {
            const inputs = item.querySelectorAll('input');
            const dependentId = item.getAttribute('data-dependent-id');
            dependents.push({
                DependentID: dependentId ? parseInt(dependentId) : 0,
                FullName: inputs[0]?.value || '',
                Relationship: inputs[1]?.value || '',
                PassportNumber: inputs[2]?.value || ''
            });
        });

        const formData = {
            Shareholder: {
                ShareholderID: parseInt(document.getElementById('edit_shareholderId')?.value) || 0,
                FirstName: document.getElementById('edit_firstName')?.value?.trim(),
                LastName: document.getElementById('edit_lastName')?.value?.trim(),
                Country: document.getElementById('edit_country')?.value,
                FolioID: document.getElementById('edit_folionId')?.value?.trim(),
                FullName: document.getElementById('edit_fullName')?.value?.trim(),
                Address1: document.getElementById('edit_address1')?.value?.trim(),
                Address2: document.getElementById('edit_address2')?.value?.trim(),
                City: document.getElementById('edit_city')?.value?.trim(),
                CompanyIndividual: document.getElementById('edit_companyIndividual')?.value,
                NoOfShares: parseInt(document.getElementById('edit_noOfShares')?.value) || 0,
                NoOfTicketsIssued: parseInt(document.getElementById('edit_noOfTickets')?.value) || 0,
                Entitlement: entitlementValue
            },
            Passports: passports,
            Dependents: dependents
        };

        // Show loading state
        const updateBtn = document.querySelector('#editShareholderModal .btn-save');
        const originalText = updateBtn.textContent;
        updateBtn.textContent = 'Updating...';
        updateBtn.disabled = true;

        console.log('Updating shareholder with passports and dependents:', formData);

        // Send data to server
        fetch('@Url.Action("UpdateShareholderWithDetails", "ShareholderListPage")', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify(formData)
        })
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                // Success - close modal and reload to show notification
                closeEditModal();
                window.location.reload();
            } else {
                // Error - keep modal open, add URL parameter to show modal after reload
                const currentUrl = new URL(window.location.href);
                currentUrl.searchParams.set('showEditModal', 'true');
                currentUrl.searchParams.set('editFolioId', formData.Shareholder.FolioID);
                currentUrl.searchParams.set('formError', 'true');
                window.location.href = currentUrl.toString();
            }
        })
        .catch(error => {
            console.error('Error updating shareholder:', error);
            // Network error - reload with modal parameter
            const currentUrl = new URL(window.location.href);
            currentUrl.searchParams.set('showEditModal', 'true');
            currentUrl.searchParams.set('editFolioId', formData.Shareholder.FolioID);
            currentUrl.searchParams.set('formError', 'true');
            window.location.href = currentUrl.toString();
        });
    }

    // Check URL parameters to reopen modal after error
    document.addEventListener('DOMContentLoaded', function() {
        // Format entitlement number input to 2 digits
        const editEntitlementNumberInput = document.getElementById('edit_entitlementNumber');
        if (editEntitlementNumberInput) {
            editEntitlementNumberInput.addEventListener('blur', function() {
                if (this.value) {
                    const num = parseInt(this.value) || 0;
                    if (num > 99) {
                        this.value = 99;
                    } else if (num < 0) {
                        this.value = 0;
                    }
                    this.value = num.toString().padStart(2, '0');
                }
            });
        }

        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('showEditModal') === 'true') {
            const folioId = urlParams.get('editFolioId');
            if (folioId) {
                // Reopen the Edit modal with the shareholder data
                loadShareholderData(folioId);
            }
            
            // Clean up URL
            const newUrl = new URL(window.location.href);
            newUrl.searchParams.delete('showEditModal');
            newUrl.searchParams.delete('editFolioId');
            newUrl.searchParams.delete('formError');
            window.history.replaceState({}, '', newUrl.toString());
        }
    });
</script>
